# 随机方案变更说明（待统计师确认）

**变更日期**：2026-08-29
**变更内容**：随机分组的分层单位由「中心」改为「医生账号」
**系统**：多中心随机分组系统（v2026.07.13-r4 基础上的改造）

---

## 一、变更前（基线状态）

- 随机单位：**中心**。同一中心的医生账号共享一条中心级序列，先来先得。
- 序列生成：每中心一次性预生成 50 例的置换区组（A/B 各 25，区组长度混合 4 和 6，当时无固定随机种子机制）。
- 实际数据：7 个中心中，仅 1 个中心（账号 `doctor1`）产生过随机记录，共 50 例（49 例有效 + 1 例作废），其余 6 个中心零随机。

## 二、变更后（目标状态）

- 随机单位：**医生账号**。每个账号一条独立序列，同一中心多个医生互不占用对方名额。
- 种子机制：**中心种子 + 账号派生**。中心保留一个固定随机种子（由独立随机化人员生成保管，入组医生不可见），每个账号的序列由「中心种子 + 账号名」经 sha256 确定性 PRNG 派生（domain separation），给定（种子、账号、区组序号）即可复现、可审计。
- 区组：A/B 1:1 置换区组，长度每次独立地在 4 和 6 之间随机选择；当前区组用尽自动生成下一个，**不设固定总例数上限**。
- 补种子：原 7 个中心均无种子，本次迁移时由系统用加密随机数各生成一个种子，存库保管（仅管理员可通过原始随机化文档导出）。
- 防猜：不再向入组医生下发随机序号（记录表、CSV、随机响应均隐藏），仅管理员可见。

## 三、方法学影响（需统计师重点确认）

1. **分层因子变更**：随机化分层单位从中心改为医生账号，属方案级变更（protocol amendment）。医生账号数多于中心数，分层变细；若多名医生各自留有未跑完的区组，全试验最大失衡会从中心级（约 3 例）放大到约「医生数 × 3」例。
2. **历史计数不结转**：`doctor1` 已产生的 50 例历史分组（A/B 恰为 25:25）在迁移后**不再结转**，其未来的新患者将从账号派生序列的第 0 个区组重新开始。其余 6 个中心同理从第 0 个区组开始。
3. **取消中心例数上限**：原每中心 50 例上限取消，改为按账号不封顶。
4. **区组长度分布改变**：由「一次洗好的固定长度多重集」改为「每个区组独立 i.i.d. 抽 4 或 6」，预期区组长度分布有变化，属非偏倚性改变。
5. **隐匿性下降**：开放标签下，医生可看到自己每一例的分组与时间顺序，区组末尾 1–2 例仍有被推断的可能（隐藏序号仅降低显式边界信息，无法完全消除）。

## 四、已保留不动的部分

- 已随机患者的分组结果、随机时间、患者资料、作废审计**全部原样保留**，迁移只重排序号、不改结果。
- 知情同意确认、入组资格确认、受试者状态管理、哈希链审计日志等既有功能全部保留。

## 五、需统计师/方案负责人签字确认

- [ ] 确认分层单位改为医生账号（记录变更日期）。
- [ ] 确认取消每中心 50 例上限。
- [ ] 确认历史 50 例 A/B 计数不结转、各账号从新区组重新开始。
- [ ] 确认区组长度分布改为每块独立抽 4/6。
- [ ] 确认种子补录方式（系统自动生成并存库保管）。

签字人：__________　日期：__________
